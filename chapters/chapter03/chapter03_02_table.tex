\begin{xltabular}{\textwidth}{|p{.4\textwidth}|Y|Y|Y|Y|}
\caption{Überblick der Azure Dienste in Bezug auf die Anforderungen (\cmark: Kann Anforderung erfüllen; \xmark: Kann Anforderung nicht erfüllen)} \label{table:eva} \\

\hline 
\diagbox[innerwidth=\hsize]{{\textbf{Anforderung}}}{\textbf{Dienst}}
& \rotatebox[origin=c]{90}{\textbf{Table Storage}}
& \rotatebox[origin=c]{90}{\textbf{SQL}}
& \rotatebox[origin=c]{90}{\textbf{Cosmos DB}}
& \rotatebox[origin=c]{90}{\textbf{ Data Lake Gen 2 }}
\hline \endfirsthead

%\diagbox[width=10em]{Diag\\Column Head I}{Diag Column\\Head II} & \textbf{Table Storage} & \textbf{SQL} & \textbf{Cosmos DB} \\
% \textbf{Anforderungen} & \textbf{Table Storage} & \textbf{SQL} & \textbf{Cosmos DB} \\
% \hline

\nameref{sec:anforderungsspezifikation:datenintegrationOnPremDB} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:datenintegrationCloudDB}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:datenintegrationREST}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:dauerhaftesSpeichern}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:datentransformation}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:datenanalysePythonUndR}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:reports}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:selfServiceReports}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:datenflussDokumentation} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:DatenKlassifizierung} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:vielfältigeVisualisierungsmöglichkeiten} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:SchnelleDatenintegration} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:schnelleAntwortzeitenDerReports} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:Datenkonsistenz} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:verfügbarkeit} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:AutomatischeFehlerbehandlung} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:speicherkapazität}
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:QuellsystemeÄndern} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:skalierungDerSpeicherkapazität} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:langlebigkeit} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:fehlerquellenIdentifizieren} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:einheitlicheTechnologie} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:rbac} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:SAG_AD} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:löschenKundendaten} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:zugriffStandort} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:löschenKundendaten} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:verschlüsselung} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\nameref{sec:anforderungsspezifikation:dsgvo} 
& ? % Table Storage
& ? % SQL
& ? % Cosmos DB
& ? % Data Lake Gen 2
\\ \hline

\hline
\end{xltabular}